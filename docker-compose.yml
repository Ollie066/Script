services:
  server:
    build: ./server
    ports: ["4000:4000"]
    environment: [PORT=4000]
  frontend:
    build: { context: ./frontend, args: [VITE_API_BASE=http://localhost:4000/api/v1, VITE_WS_URL=ws://localhost:4000/ws] }
    ports: ["8080:80"]
    depends_on: [server]
